// CALCULATOR

// Getting Informations

const x = parseInt(prompt("Digite o primeiro numero da equação: "));
const y = parseInt(prompt("Digite o segundo numero da equação: "));

// Operations

const sum = x + y;
const subtraction = x - y;
const multiplication = x * y;
const division = x / y;

// Showing The Results

alert(
    "Resultados\n" +
    "Soma = " + sum +
    "\nSubtração = " + subtraction +
    "\nMultiplicação = " + multiplication +
    "\nDivisão = " + division
);

/*console.log("Soma: ", x + y)
console.log("Subtração: ", x - y)
console.log("Multiplicação: ", x * y)
console.log("Divisão: ", x / y)*/